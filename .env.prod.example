# Variables para despliegue en servidor (opcional)
# Copia a .env junto a docker-compose.prod.yml

POSTGRES_USER=postgres
POSTGRES_PASSWORD=cambia-esta-clave
POSTGRES_DB=mainstage
JWT_SECRET=cambia-este-secreto
APP_PORT=8080
SEED_ON_START=false
